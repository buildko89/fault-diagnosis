"""Streamlit GUI for the fault-diagnosis tool.

The page is built in ``app.py`` (run with ``streamlit run fault_gui/app.py`` or
the ``fault-gui`` launcher). Submodules here avoid importing Streamlit at
package import time so that ``fault_gui.plots`` stays usable on its own.
"""
